// examples/demo.ltl — runnable demo for ltl_gb_cpu

import std::io
use ltl_gb_cpu as lib

fn main() {
    io::println("ltl_gb_cpu demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}