// ltl-gb-cpu/main.ltl
//
// Game Boy SM83 CPU core + MBC1 cartridge loader.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-gb-cpu v0.1.0")
    io::println("Game Boy SM83 CPU core + MBC1 cartridge loader.")
    0
}
